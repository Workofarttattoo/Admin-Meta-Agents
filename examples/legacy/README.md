# Legacy Agent Prototypes

These files are early prototypes from before the codebase was refactored into the `admin_meta_agents` package. They reference an undefined `Agent` class and **will not run** as-is.

They are preserved here for reference only. For working examples, see:

- `admin_meta_agents/examples.py` — built-in agents and demo workflow
- `main.py` — CLI entry point that runs the default workflow
